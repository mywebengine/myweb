0)!!! заменить передачу attr в renderTag getScope ... на str - перенести логику по СТР из ГЕРСКОПА в РЕНДЕРТАГ 
-- сейчас мы для ренджера готовим скоп и атрибуты, а надо будет сделать что мы готовим скоп родителя и передаем стр - после чего дополняем скоп

2) 	<link _inc.u="../MySNT/list.html" slot="main" />
	222<link _inc.u="../page/vip.html" slot="main" />11

	-- исчезают 222 и 111

3) _if _for
	_if _for
	_else

4) ??? _fetch _for
	_inc _for

5) _if _inc
	_inc _if ?

7) inc default slot

8) <div _inc.u="/ctrl/edit.htm" _if - если удалить if то непонятно выводится ошибка
					data-id="email"
					data-_label="locale.sboFiatForm.email"
					data-_value="self.data.user?.email"
					data-placeholder="info@gmail.com"
					data-type="email"
					data-required=""></div>
				<input _else="" type="hidden" name="email" _attr.value="self.data.user.email" />

10) dispatchEvent on <template> - dont work - думаю так и должно быть

11) удаление ликов с цсс при удалениии старого
- скрипты всегда выполняются при вставке, если они уже были загружены то там или кэш или import() вернет промис уже разрешенный
- проблема у счётчике, когда есть ку клонирования - нужно в куКлоне менять счётчик

13) документация
14) сервер
	+17) $i[p_topUrl]

15) import min21